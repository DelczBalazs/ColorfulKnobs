/**
 * jsui knob drawing, palette popup, and hit-testing — Phase 3.
 * All paints and LOM writes must be deferred off the high-priority thread.
 * Drawing side effects are isolated here; color decisions come from state.ts.
 */

export {};
